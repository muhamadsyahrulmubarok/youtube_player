# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently empty (only a `README.md` placeholder). There are no services, dependencies, or build steps to run.

- **No package manager or language runtime** is required at this time.
- **No services** need to be started.
- **No tests, lint checks, or build commands** exist yet.

When application code is added, update this section with:
- The language/runtime and package manager used
- How to install dependencies
- How to run lint, tests, and the dev server
- Any non-obvious environment setup caveats
